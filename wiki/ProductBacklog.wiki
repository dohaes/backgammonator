#summary Product backlog document of the project
#labels Featured

Actors: program, contest, contestant manager

|| # || *User story* || *Estimation* ||
|| 1 || As a program<br />I want to use an inteface<br />so that I can interact with the system || 1 ||
|| 2 || As a contestant<br />I want the system to support Java for implementations of the AI player<br />so that I can implement my program in Java || 2 ||
|| 3 || As a contestant<br />I want to be use the Backgamminator Library<br />so that I can test my AI players locally || 5 ||
|| 4 || As a contest manager<br />I want the system to have algorithms that implement tournaments schemas<br />so that I can run a tournament and see the results || 5 ||
|| 5 || As a contest manager<br />I want the system to provide systematized results<br />so that I can analyze them || 1 ||
|| 6 || As a contestant<br />I want the system to validate/verify my program<br />so that I can be aware if there are any errors || 3 ||
|| 7 || As a contest manager<br />I want the system to provide me with an user interface<br />so that I can run the tournament via it || 1 ||
|| 8 || As a contestant<br />I want to upload the source code of my program via web interface<br />so that I can take part in the tournament || 3 ||
|| 9 || As a contest manager<br />I want the contestants' programs to be granted a limited set of permissions<br />so that no malicious code can be executed and  the system stays secured || 4 ||
|| 10 || As a contest manager<br />I want to adjust the tournament settings<br />so that I can define tournament type, number of battles and participating players || 2 ||
|| 11 || As a contestant <br />I want the system to provide sample AI players<br />so that I can explore their source code ot run them against my AI players || 5 ||
|| 12 || As contestant<br />I want to log in to the system<br />so that I am authenticated || 1 ||
|| 13 || As contestant manager<br />I want to log in the system<br />so that I am authenticated || 1 ||
|| 14 || As contestant<br />I want to register into the system<br />so that I can log in || 1 ||
|| 15 || As a contestant<br />I want the system to support C/C++ for implementations of the AI player<br />so that I can implement my program in C/C++ || 2 ||
|| 16 || As a contest manager<br />I want to adjust the uploading settings<br />so that I can define period for uploading and limit for the size ot the file to be uploaded || 1 ||
|| 17 || As contest manager<br />I want to delete registrations<br />so that I can remove contestants' accounts || 1 ||