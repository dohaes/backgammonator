<project name="Backgammonator Project" default="lib" basedir=".">
	<description>
        simple example build file
    </description>
	<!-- set global properties for this build -->
	<property name="project.home" location="../" />
	<property name="src" location="${project.home}/src" />
	<property name="build" location="${project.home}/.classes" />

	<target name="lib" depends="distLib" />
	
	<target name="init">
		<!-- Create the time stamp -->
		<tstamp />
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build}" />
	</target>

	<target name="compile" depends="init" description="compile the source ">
		<!-- Compile the java code from ${src} into ${build} -->
		<javac srcdir="${src}" destdir="${build}" />
	</target>

	<target name="distLib" depends="compile" description="generate the distribution">
		<!-- Create the distribution directory -->
		<mkdir dir="${project.home}/lib" />

		<jar jarfile="${project.home}/lib/backgammonlibrary.jar" basedir="${build}"
		excludes=
			"backgammonator/impl/protocol/*
			"/>
	</target>

	<target name="clean" description="clean up">
		<!-- Delete the ${build} directory trees -->
		<delete dir="${build}" />
	</target>
</project>