<project name="Backgammonator Project" default="clean" basedir=".">
    <description>
        simple example build file
    </description>
  <!-- set global properties for this build -->
  <property name="project.home"  location="../"/>
  <property name="src" location="${project.home}/src"/>
  <property name="build" location="${project.home}/.classes"/>

  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source " >
    <!-- Compile the java code from ${src} into ${build} -->
    <javac srcdir="${src}" destdir="${build}"/>
  </target>

  <target name="dist" depends="compile"
        description="generate the distribution" >
    <!-- Create the distribution directory -->
    <mkdir dir="${project.home}/lib"/>

    <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
    <jar jarfile="${project.home}/lib/BackgammonatorLib.jar" basedir="${build}"/>
  </target>

  <target name="clean"
        description="clean up" depends="dist">
    <!-- Delete the ${build} directory trees -->
    <delete dir="${build}"/>
  </target>
</project>